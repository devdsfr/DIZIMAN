spring.datasource.url=jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:diziman}
spring.datasource.username=${PGUSER:postgres}
spring.datasource.password=${PGPASSWORD:1254}
spring.jpa.hibernate.ddl-auto=update
server.port=${PORT:8080}

# CORS
frontend.url=${FRONTEND_URL:http://localhost:4200}

# JWT
jwt.secret=${JWT_SECRET:diziman-super-secret-key-2024-must-be-at-least-32-chars-long}
jwt.expiration=86400000
